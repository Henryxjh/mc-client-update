plugins {
    id 'base'
}

tasks.register('buildAll') {
    group = 'build'
    description = 'Builds the Fabric and NeoForge distributions.'
    dependsOn ':common:test', ':fabric:build', ':neoforge:build'
}
